# Release Highlights

Fix tag search in the Aside index sidebar so its results match the notes and tags shown in the index.

## Highlights

- Search only notes represented in the Aside index, excluding the generated index note itself.
- Include both source-note tags and tags from Aside comments, with accurate counts and deduplicated file results.
- Refresh tag metadata after startup so cached notes are not missing from searches.

## Notes

- Release assets are limited to `main.js`, `manifest.json`, and `styles.css`.
