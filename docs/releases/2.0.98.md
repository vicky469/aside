# 2.0.98

## What's new

- Publish Markdown notes as rendered HTML instead of exposing raw Markdown files.
- Keep Markdown publish actions, public URLs, and cache purges aligned with the generated `.html` artifact.
- Reject raw Markdown publish artifacts so public publishing only ships HTML or PDF content.
- Hide Aside index tag matches from thought-trail related-file suggestions.

## Notes

- No user migration is required.
- Release artifacts remain limited to `main.js`, `manifest.json`, and `styles.css`.
