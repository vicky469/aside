# 2.0.96

## What's new

- Render agent-run file metadata as clickable Obsidian-style links that show the file name.
- Add a footer collapse control for agent-run metadata, aligned with the share and add-to-thread actions.
- Keep thread footer action buttons visible without requiring the card to be active.
- Show purple, text-only `Copied` feedback after sharing a side note.

## Notes

- No user migration is required.
- Release artifacts remain limited to `main.js`, `manifest.json`, and `styles.css`.
