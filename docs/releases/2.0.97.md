# 2.0.97

## What's new

- Show agent metadata skills as a simple `Skills` row while keeping the built-in Aside wrapper hidden.
- Record requested domain skills for agent runs, including Excalidraw drawing work and Obsidian canvas board work.
- Preserve runtime-reported skills when Codex or Claude emits more detailed skill metadata.
- Keep default current-file metadata out of the visible agent metadata rows.

## Notes

- No user migration is required.
- Release artifacts remain limited to `main.js`, `manifest.json`, and `styles.css`.
